#!/bin/bash

DEBUG=${DEBUG-false}

if [[ "$DEBUG" == true ]] ; then
  exec 3>&1
else
  exec 3>&1 &>/dev/null
fi

echo "  -- Building $NAME (${LAUNCHER_DIR}/Dockerfile)" >&3
(cd ${LAUNCHER_DIR} && docker build -t $NAME .)
